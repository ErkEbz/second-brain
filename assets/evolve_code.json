{
  "type": "object",
  "properties": {
    "file_path": {
      "type": "string",
      "description": "Path to the local skill file to modify, for example SKILL.md or scripts/index.html."
    },
    "new_code": {
      "type": "string",
      "description": "New full file contents that will replace the current file."
    },
    "reason": {
      "type": "string",
      "description": "Explanation of why the change is needed."
    }
  },
  "required": ["file_path", "new_code", "reason"]
}
