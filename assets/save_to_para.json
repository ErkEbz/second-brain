{
  "type": "object",
  "properties": {
    "category": {
      "type": "string",
      "enum": ["Projects", "Areas", "Resources", "Archives"],
      "description": "PARA category where the note should be saved."
    },
    "content": {
      "type": "string",
      "description": "Cleaned and structured note content to save."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Tags used for later search and retrieval."
    }
  },
  "required": ["category", "content"]
}
