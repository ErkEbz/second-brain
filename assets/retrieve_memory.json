{
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "Search query or semantic concept to look up in the knowledge base."
    },
    "category_filter": {
      "type": "string",
      "enum": ["All", "Projects", "Areas", "Resources", "Archives"],
      "description": "Optional PARA category filter. Use 'All' if unsure."
    }
  },
  "required": ["query"]
}
